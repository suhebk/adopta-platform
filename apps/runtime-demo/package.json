{
  "name": "@adopta/runtime-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @adopta/runtime-sdk build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @adopta/runtime-sdk build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @adopta/runtime-sdk build && vitest run"
  },
  "dependencies": {
    "@adopta/runtime-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
